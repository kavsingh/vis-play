extends:
  - apps/*/lefthook.yml
  - packages/*/lefthook.yml

pre-push:
  parallel: true
  jobs:
    - run: pnpm lint
      glob: "**/*.{ts,tsx,js,jsx}"
    - run: pnpm tombi lint
      glob: "**/*.toml"
