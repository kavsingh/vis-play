extends:
  - apps/*/lefthook.yml
  - packages/*/lefthook.yml

pre-push:
  parallel: true
  jobs:
    - run: bun lint
      glob: "**/*.{ts,tsx,js,jsx}"
    - run: bun tombi lint
      glob: "**/*.toml"
